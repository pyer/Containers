# Alpine custom profile
export CHARSET=UTF-8
export PATH=/usr/sbin:/usr/bin:/sbin:/bin
export PAGER=less
export PS1='\h:\w\$ '
umask 022

for script in /etc/profile.d/*.sh ; do
  [ -r $script ] && . $script
done
